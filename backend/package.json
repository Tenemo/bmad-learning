{
  "name": "@app/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "preview": "node dist/server.js",
    "serve:test": "cross-env PORT=4173 STATIC_ROOT=../frontend/dist TODO_DB_PATH=.data/preview.sqlite node dist/server.js",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/static": "^8.2.0",
    "better-sqlite3": "^12.2.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.14.1",
    "@vitest/coverage-v8": "^3.1.1",
    "tsx": "^4.19.3",
    "vitest": "^3.1.1"
  }
}
